{
  "name": "@retrojb/figma-bridge-plugin",
  "version": "0.0.0",
  "description": "Figma desktop bridge plugin for the Figma Console MCP harness, with reconnect, connectivity status, live selection, and in-canvas node highlighting.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "keywords": [
    "figma",
    "figma-plugin",
    "mcp",
    "bridge"
  ],
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "check-types": "tsc --noEmit -p tsconfig.sandbox.json && tsc --noEmit -p tsconfig.ui.json && tsc --noEmit -p tsconfig.tools.json",
    "lint": "eslint . --max-warnings 0",
    "test": "node --test tests/*.test.mjs",
    "mock-server": "node tools/mock-mcp-server.mjs",
    "xpublish": "npm run publish"
  },
  "devDependencies": {
    "@figma/plugin-typings": "1.136.0",
    "@retrojb/eslint-config": "*",
    "@retrojb/typescript-config": "*",
    "@types/node": "26.3.0",
    "esbuild": "0.28.2",
    "eslint": "10.9.1",
    "typescript": "6.0.3",
    "ws": "8.21.3"
  },
  "dependencies": {
    "@retrojb/plugin-kit": "*"
  }
}
